import React, { useState } from "react";
import { Button } from "react-bootstrap";

const ShelfItem = ({ productid }) => {
  const [qtyState, setQtyState] = useState(1);
  return <div></div>;
};

export default ShelfItem;
